<div class="max-w-4xl mx-auto p-6 bg-gray-90 rounded-lg shadow-lg border border-gray-200 mt-8">
  <h1 class="animation-fadeIn1 text-5xl font-semibold text-center text-gray-800 mb-4">診断結果</h1>
  <div class="animation-fadeIn2">
    <div class=" flex justify-center mb-6">
      <%= image_tag @result.image_path, alt: "診断結果画像", class: "w-64 h-64 object-cover rounded-full border-4 border-gray-200 shadow-md", style: " object-position: center 10%;" %>
    </div>

    <div class="text-center">
      <p class="text-2xl font-bold text-gray-900 mb-2">
        <%= @result.name %>
      </p>
      <p class="text-lg font-medium text-gray-900 mb-2">
        <%= @result.title %>
      </p>
      <p class="text-lg font-medium text-black-900 mb-2">
        <%= @result.description %>
      </p>
      <p class="text-lg font-semibold text-gray-900 mb-2 mt-6">
        <%= @result.name %>とは
      </p>
      <p class="text-lg font-medium text-gray-900 mb-2">
        <%= @result.monk_description %>
      </p>

      <% if user_signed_in? %>
        <div class="">
          <%= link_to "もう一度診断する", root_path, class: 'mt-9 inline-block bg-blue-200 hover:bg-blue-400 text-black font-semibold py-3 px-6 rounded-md shadow-md transition duration-300 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:ring-opacity-50' %>
        </div>
      <% else %>
       
          <%= link_to "もう一度診断する", root_path, class: 'mt-9 inline-block bg-blue-200 hover:bg-blue-400 text-black font-semibold py-3 px-6 rounded-md shadow-md transition duration-300 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:ring-opacity-50' %>
        <div class="pt-5 hover:font-bold transition-all duration-400">
          <%= link_to "ユーザー登録であなたの訪れた場所を共有する", new_user_registration_path %>
        </div>
      <% end %>

    </div>
  </div>

  <div class="flex justify-end py-6">
    <div>
      <p class="text-sm  py-2 px-2">結果をシェアする</p>
    </div>
    <a href="https://twitter.com/intent/tweet?text=あなたの診断結果は...%0A%0A<%= ERB::Util.url_encode(@result.name) %>(<%= ERB::Util.url_encode(@result.title) %>)%0A&hashtags=診断,名僧,歴史&url=<%= ERB::Util.url_encode(root_url) %>"
        target="_blank" rel="nofollow">
      <i class="fa-brands fa-square-x-twitter fa-2x"></i>
    </a>
  </div>
</div>
